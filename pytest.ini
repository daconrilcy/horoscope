# Objectif du fichier: Décrire l'objectif de pytest.ini
# TODO: compléter cette description.

[pytest]
testpaths = backend/tests
# Objectif du fichier: configuration de Pytest (chemins, options par défaut).
